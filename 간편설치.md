# 무료로 서버 올리기 — 따라만 하면 됩니다

내 컴퓨터에 아무것도 설치하지 않고, **웹사이트 세 곳만 가입**하면 끝납니다.
비용은 **0원**이고, 30~40분 걸립니다.

| 무엇 | 어디 | 비용 |
|---|---|---|
| 데이터베이스 | Neon | 무료 |
| 서버 | Render | 무료 |
| 코드 보관 | GitHub | 무료 |

---

## 1단계 · GitHub에 코드 올리기 (10분)

1. **github.com** 가입 (구글 계정 가능)
2. 오른쪽 위 **＋** → **New repository**
3. 이름은 `vlm-guard-server`, **Private** 선택 → **Create**
4. 다음 화면에서 **uploading an existing file** 링크 클릭
5. `서버` 폴더 안의 **모든 파일과 폴더를 통째로 끌어다 놓기**
6. 아래 **Commit changes** 클릭

> ⚠️ `서버` 폴더 자체가 아니라 **그 안의 내용물**을 올려야 합니다.
> `package.json` 이 목록 맨 위에 보이면 제대로 된 겁니다.

---

## 2단계 · 데이터베이스 만들기 (5분)

1. **neon.com** 가입 (GitHub 계정으로 로그인하면 편함)
2. **Create project** → 이름 아무거나, 지역은 **Singapore** 선택
3. 만들어지면 **Connection string** 이 보입니다
4. `postgresql://...` 로 시작하는 **긴 주소를 복사**해서 메모장에 붙여두세요

> 이게 `DATABASE_URL` 입니다. 잠시 뒤에 씁니다.

---

## 3단계 · Claude API 키 준비

1. **console.anthropic.com** 로그인
2. **API Keys** → **Create Key** → 복사해서 메모장에 붙여두기
3. **Billing** 에서 카드 등록 후 소액($5) 충전

> 이 키는 이제 **서버에만** 들어갑니다. 앱에는 넣지 않습니다.

---

## 4단계 · 서버 올리기 (10분)

1. **render.com** 가입 (GitHub 계정으로 로그인)
2. **New +** → **Blueprint** 선택
3. 1단계에서 만든 저장소 선택 → **Connect**
4. `render.yaml` 을 자동으로 읽어 설정이 채워집니다
5. 아래 두 칸만 직접 입력합니다:

| 항목 | 넣을 값 |
|---|---|
| `DATABASE_URL` | 2단계에서 복사한 Neon 주소 |
| `ANTHROPIC_API_KEY` | 3단계에서 복사한 키 |

6. **Apply** 클릭 → 3~5분 기다리면 완료

끝나면 `https://vlm-guard-server-xxxx.onrender.com` 같은 주소가 생깁니다.

> **데이터베이스 테이블은 자동으로 만들어집니다.** 명령어를 칠 필요 없습니다.

---

## 5단계 · 관리자 화면 열기

브라우저에서 주소 뒤에 `/admin.html` 을 붙여 접속합니다.

```
https://vlm-guard-server-xxxx.onrender.com/admin.html
```

1. **가입하기** 클릭 → 상호명·이메일·비밀번호·휴대폰 입력
2. 로그인되면 **기기 등록** 에서 이름을 넣고 등록
3. 화면에 뜨는 **기기 키**(`dev_...`)를 복사 — 이걸 현장 앱에 넣습니다

---

## 6단계 · 현장 앱 연결

감시카메라 앱에서 이제 API 키 대신 **기기 키**와 **서버 주소**를 넣으면 됩니다.
(앱 수정이 필요하니 요청해 주세요 — 바로 고쳐 드립니다)

---

## 무료 요금제의 한계 — 알고 쓰세요

솔직하게 말씀드립니다.

**① 15분 쉬면 서버가 잠듭니다**
Render 무료는 15분간 요청이 없으면 절전에 들어갑니다.
다시 깨어나는 데 **30~60초**가 걸려, 그 사이 첫 감지가 늦어집니다.
→ 실제 고객에게 팔 때는 유료(월 $7 정도)로 바꾸세요.

**② 감지 사진이 저장되지 않습니다**
무료 서버는 재배포할 때 파일이 지워져서, 사진 저장을 꺼두었습니다
(`SAVE_IMAGES=false`). 기록과 상황 설명은 DB에 정상적으로 남습니다.
→ 사진이 필요하면 유료 디스크나 S3 같은 저장소를 붙여야 합니다.

**③ 데이터베이스 용량 0.5GB**
감지 기록 기준 수십만 건은 들어갑니다. 당분간 충분합니다.

**④ 알림·결제는 아직 안 됩니다**
카카오톡·문자·카드결제는 각각 대행사·PG사 계약이 필요합니다.
계약 후 Render 설정에 값만 넣으면 켜집니다.

---

## 정리하면

지금 이대로 하시면 **테스트와 시연은 충분히 가능**합니다.
실제 고객에게 돈 받고 팔 단계가 되면 유료로 올리시면 되고,
그때 비용은 **월 1만원 안팎**입니다(서버 $7 + DB 무료 유지).

막히는 단계가 있으면 그 화면을 캡처해서 보내주세요. 바로 짚어드리겠습니다.
